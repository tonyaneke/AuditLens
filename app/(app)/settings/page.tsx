"use client";

// Settings & user access — React shell (migrated from viewSettings in public/audit-bot.js).
import SettingsPage from "@/components/settings/SettingsPage";

export default function Page() {
  return <SettingsPage />;
}
